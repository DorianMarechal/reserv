services:
  php:
    build: ./backend
    volumes:
      - ./backend:/app
  nginx:
    image: nginx:stable-alpine
    ports:
      - ${SERVER_PORT}:80
    volumes:
      - ./backend:/app
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php